// tailwind.config.js

const config = {
    theme: {
        extend: {
            fontFamily: {
                tiny5: ['Tiny5', 'sans-serif'],
            },
        },
    },
    plugins: [],
}

export default config
